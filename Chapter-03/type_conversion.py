# type conversion means change the type of variable in another type.
cgpa="9.8"
print(type(cgpa))  #it return string type

# but after type conversion it return float type
cgpa=float(cgpa)
print(type(cgpa))