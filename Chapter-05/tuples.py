# tuple are same as list it contain multiple data types element . but it is immutable it means it can not be changed or update after decleared.
# it is started from parenthesis

my_tuple=("hello",45,"ram",7.08,True, "Deepak") 
print(my_tuple)
print(type(my_tuple))

#📝 my_tuple[2]=92  # it gives error beacuse tuples are immutable.we can only perform operations in tuples but it can not be changed or overwrite the original tuples.  ["TypeError: 'tuple' object does not support item assignment"]
