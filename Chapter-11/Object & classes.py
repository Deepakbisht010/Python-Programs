# Class is a blueprint for creating objects. It contains attributes,and methods.
class Student:
    def printStmt(self):
        print("It is a student class")
        
        
# object:-
# object is an instance of a class. When class in defined , a template is defined  but memory allocated only after instantiation.

object = Student()
object.printStmt()


        